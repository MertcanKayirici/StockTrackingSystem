@model StockTrackingSystem.Models.Supplier

@{
    // Page title
    ViewData["Title"] = "Tedarikçi Ekle";
}

<style>
    /* =========================
           PAGE BACKGROUND
        ========================= */
    body {
        background: #f8fafc;
    }

    /* =========================
           MAIN CARDS
        ========================= */
    .page-card, .form-card {
        border: 0;
        border-radius: 24px;
        background: #ffffff;
        box-shadow: 0 10px 30px rgba(15, 23, 42, 0.06);
    }

    .page-card {
        padding: 30px 32px;
        margin-bottom: 24px;
    }

    /* =========================
           PAGE HEADER
        ========================= */
    .page-title {
        font-size: 2rem;
        font-weight: 800;
        color: #0f172a;
        margin-bottom: 6px;
    }

    .page-subtitle {
        color: #64748b;
        font-size: 1.02rem;
        margin-bottom: 0;
    }

    /* =========================
           FORM CARD
        ========================= */
    .form-card {
        overflow: hidden;
    }

    .form-header {
        padding: 24px 28px 12px 28px;
        border-bottom: 1px solid #f1f5f9;
    }

    .form-title {
        font-size: 1.15rem;
        font-weight: 800;
        color: #0f172a;
        margin-bottom: 4px;
    }

    .form-text {
        color: #64748b;
        margin-bottom: 0;
        font-size: 0.96rem;
    }

    .form-body {
        padding: 28px;
    }

    /* =========================
           FORM ELEMENTS
        ========================= */
    .form-label {
        font-size: 0.96rem;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 10px;
    }

    .form-control, textarea.form-control {
        min-height: 54px;
        border-radius: 16px;
        border: 1px solid #dbe2ea;
        padding-left: 16px;
        padding-right: 16px;
        background: #fff;
        font-size: 1rem;
        box-shadow: none;
        transition: all 0.2s ease;
    }

    textarea.form-control {
        min-height: 120px;
        padding-top: 14px;
        resize: vertical;
    }

        .form-control:focus, textarea.form-control:focus {
            border-color: #3b82f6;
            box-shadow: 0 0 0 0.22rem rgba(59, 130, 246, 0.12);
        }

    /* =========================
           VALIDATION ALERT
        ========================= */
    .alert-danger {
        border-radius: 16px;
        border: 0;
        background: #fef2f2;
        color: #b91c1c;
        padding: 14px 16px;
        margin-bottom: 20px;
    }

    /* =========================
           STATUS BOX
        ========================= */
    .status-box {
        border: 1px solid #e2e8f0;
        border-radius: 18px;
        padding: 16px 18px;
        background: #f8fafc;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 14px;
    }

    .status-box-left h6 {
        margin: 0 0 4px 0;
        font-weight: 700;
        color: #0f172a;
        font-size: 1rem;
    }

    .status-box-left p {
        margin: 0;
        color: #64748b;
        font-size: 0.9rem;
    }

    /* =========================
           MODERN SWITCH
        ========================= */
    .modern-switch {
        position: relative;
        display: inline-block;
        width: 54px;
        height: 30px;
        margin: 0;
        flex-shrink: 0;
    }

        .modern-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .modern-switch .slider {
            position: absolute;
            inset: 0;
            cursor: pointer;
            background: #e5e7eb;
            transition: .25s ease;
            border-radius: 999px;
            box-shadow: inset 0 0 0 1px rgba(15, 23, 42, 0.06);
        }

            .modern-switch .slider:before {
                content: "";
                position: absolute;
                height: 22px;
                width: 22px;
                left: 4px;
                top: 4px;
                background: white;
                transition: .25s ease;
                border-radius: 50%;
                box-shadow: 0 4px 10px rgba(15, 23, 42, 0.15);
            }

        .modern-switch input:checked + .slider {
            background: linear-gradient(135deg, #2563eb, #3b82f6);
        }

            .modern-switch input:checked + .slider:before {
                transform: translateX(24px);
            }

    .status-text {
        min-width: 56px;
        text-align: right;
        font-size: 0.92rem;
        font-weight: 800;
    }

    .status-active {
        color: #16a34a;
    }

    .status-passive {
        color: #ef4444;
    }

    /* =========================
           ACTION BUTTONS
        ========================= */
    .form-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        margin-top: 28px;
    }

    .save-btn, .cancel-btn {
        min-height: 52px;
        border-radius: 16px;
        padding: 0 24px;
        font-weight: 700;
        font-size: 0.98rem;
        display: inline-flex;
        align-items: center;
        text-decoration: none;
    }
</style>

<div class="container py-4">
    <!-- =========================
         PAGE HEADER
    ========================= -->
    <div class="page-card">
        <div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-center gap-3">
            <div>
                <h1 class="page-title">Yeni Tedarikçi Ekle</h1>
                <p class="page-subtitle">Yeni bir tedarikçi oluşturarak ürünlerini daha düzenli yönetebilirsin.</p>
            </div>

            <a asp-action="Index" class="btn btn-outline-secondary cancel-btn">
                <i class="bi bi-arrow-left me-2"></i>Listeye Dön
            </a>
        </div>
    </div>

    <!-- =========================
         FORM CARD
    ========================= -->
    <div class="form-card">
        <div class="form-header">
            <div class="form-title">Tedarikçi Bilgileri</div>
            <p class="form-text">Firma ve iletişim bilgilerini doldurarak kayıt oluştur.</p>
        </div>

        <div class="form-body">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                <div class="row g-4">
                    <!-- Company name -->
                    <div class="col-md-6">
                        <label asp-for="CompanyName" class="form-label">Firma Adı</label>
                        <input asp-for="CompanyName" class="form-control" placeholder="Örn: Tekno Tedarik A.Ş." />
                        <span asp-validation-for="CompanyName" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <!-- Contact person -->
                    <div class="col-md-6">
                        <label asp-for="ContactName" class="form-label">Yetkili Kişi</label>
                        <input asp-for="ContactName" class="form-control" placeholder="Örn: Ahmet Yılmaz" />
                        <span asp-validation-for="ContactName" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <!-- Phone -->
                    <div class="col-md-6">
                        <label asp-for="Phone" class="form-label">Telefon</label>
                        <input asp-for="Phone" class="form-control" placeholder="0555 000 00 00" />
                        <span asp-validation-for="Phone" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <!-- Email -->
                    <div class="col-md-6">
                        <label asp-for="Email" class="form-label">E-posta</label>
                        <input asp-for="Email" class="form-control" placeholder="info@firma.com" />
                        <span asp-validation-for="Email" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <!-- Address -->
                    <div class="col-12">
                        <label asp-for="Address" class="form-label">Adres</label>
                        <textarea asp-for="Address" class="form-control" rows="4" placeholder="Firma adresini giriniz..."></textarea>
                        <span asp-validation-for="Address" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <!-- Supplier status -->
                    <div class="col-12">
                        <label class="form-label">Durum</label>

                        <div class="status-box">
                            <div class="status-box-left">
                                <h6>Tedarikçi Durumu</h6>
                                <p>Yeni tedarikçi aktif olarak oluşturulsun mu?</p>
                            </div>

                            <div class="d-flex align-items-center gap-3">
                                <label class="modern-switch">
                                    <input asp-for="IsActive" id="isActiveSwitch" />
                                    <span class="slider"></span>
                                </label>

                                <span id="statusLabel" class="status-text @(Model.IsActive ? "status-active" : "status-passive")">
                                    @(Model.IsActive ? "Aktif" : "Pasif")
                                </span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Form actions -->
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary save-btn">
                        <i class="bi bi-check2-circle me-2"></i>Kaydet
                    </button>

                    <a asp-action="Index" class="btn btn-outline-secondary cancel-btn">
                        <i class="bi bi-x-circle me-2"></i>Vazgeç
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        // =========================
        // STATUS SWITCH ELEMENTS
        // =========================
        const isActiveSwitch = document.getElementById("isActiveSwitch");
        const statusLabel = document.getElementById("statusLabel");

        // Update active/passive label
        function updateStatusLabel() {
            if (!isActiveSwitch || !statusLabel) return;

            if (isActiveSwitch.checked) {
                statusLabel.textContent = "Aktif";
                statusLabel.classList.remove("status-passive");
                statusLabel.classList.add("status-active");
            } else {
                statusLabel.textContent = "Pasif";
                statusLabel.classList.remove("status-active");
                statusLabel.classList.add("status-passive");
            }
        }

        // Initialize and bind switch event
        if (isActiveSwitch) {
            updateStatusLabel();
            isActiveSwitch.addEventListener("change", updateStatusLabel);
        }
    </script>
}